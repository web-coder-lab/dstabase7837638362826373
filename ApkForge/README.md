# ApkForge Database Folder

**DO NOT touch other folders in this repo.**

- `Users.json` — device/app tokens + build history
- `builds.json` — optional build index

Server only writes here. No APK build/host in this folder.
