# chessking/ — Genius Clan durable database

Repo: `dstabase7837638362826373`  
Folder: **`chessking/`** only (other folders in this repo are unrelated — do not touch).

| Collection | Path |
|------------|------|
| users | `chessking/users/{id}.json` |
| sessions | `chessking/sessions/{id}.json` |
| inventory | `chessking/inventory/{user_id}.json` |
| wallet | `chessking/wallet/{user_id}.json` |
| daily_rewards | `chessking/daily_rewards/{user_id}.json` |
| register_intents | `chessking/register_intents/{id}.json` |
| matches | `chessking/matches/{id}.json` |
| indexes | `chessking/indexes/*.json` |

API env:
```
GITHUB_DATA_OWNER=web-coder-lab
GITHUB_DATA_REPO=dstabase7837638362826373
GITHUB_DATA_ROOT=chessking
GITHUB_DATA_BRANCH=main
GITHUB_DATA_TOKEN=<PAT>
```
