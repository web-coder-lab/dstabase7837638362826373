{
  "description": "Schema only — do not put real secrets here",
  "fields": {
    "username": "string unique",
    "password_hash": "bcrypt hash",
    "role": "superadmin | admin | moderator | user"
  }
}
